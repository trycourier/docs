---
sidebar_position: 2
---

import ApiReferenceUpdate, {
  ApiReferenceEndPoint,
  ApiReferenceMeta,
  ApiReferencePathParams,
  ApiReferenceBodyParam,
} from "@site/src/components/ApiReferenceUpdate";
import config from "../configs/lists/replace";

# Update a List

:::info List_id is not reuseable

A deleted `list_id` can be restored via the [Restore a List API](restore.mdx) and once deleted **cannot be used to create a new list**.

:::

<ApiReferenceMeta {...config} />
<ApiReferenceEndPoint {...config} />
<ApiReferencePathParams {...config} />
<ApiReferenceBodyParam {...config} />

# Example

Method: `PUT`

URL: `https://api.courier.com/lists/abcdefgh12345678`

Body:
​

```json
{
  "name": "Example List Name",
  "preferences": {
    "notifications": {
      "abcdefgh12345678": {
        "channel_preferences": [
          {
            "channel": "direct_message"
          }
        ],
        "rules": [
          {
            "type": "snooze"
          }
        ],
        "status": "OPTED_IN"
      }
    },
    "categories": {
      "cooking": {
        "channel_preferences": [
          {
            "channel": "direct_message"
          }
        ],
        "rules": [
          {
            "type": "snooze"
          }
        ],
        "status": "OPTED_IN"
      }
    }
  }
}
```
